apiVersion: 1

datasources:
  - name: CloudCost Prometheus
    uid: cloudcost-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
